# TimeTable

茨木〜神戸方面のCSV時刻表を検索し、行き・帰りの列車をダイヤグラムで表示するFlaskアプリです。

## 起動（Windows / Python導入済みの環境）

リポジトリのフォルダで以下を実行します。

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m flask --app app run --port 5050
```

ブラウザで http://127.0.0.1:5050 を開きます。停止は起動した端末で Ctrl+C。
既に仮想環境を作成済みなら、最後のコマンドだけで再起動できます。

## 乗換候補の操作

1. 「行き」で希望到着時刻（例：09:15）、または「帰り」で神戸の希望出発時刻（例：18:00）を検索します。
2. 列車線または駅箱をクリックすると、乗車中の列車を選択します。
3. 別列車の駅箱をクリックすると、乗車中の列車の同じ駅から赤線を引きます。
4. 「乗換解除」は乗車中を残して候補だけ解除します。「選択解除」は両方解除します。
5. 列車線をクリックすると乗車中の列車を変更できます。

乗車中の列車に同じ駅箱がない場合は候補にできません。赤線は手動選択の表示であり、
乗換可否・必要乗換時間を判定しません。時刻が逆順の候補も選択できます。
到着時刻がCSVにない駅では「着 —」と表示します。

## 2026-09-10の引き継ぎ

- 取得元： https://github.com/user06-practice/TimeTable
- 基準コミット： `864cb5d`（列車選択・ズームまで実装済み）
- TimeTableの会話でpush後に提示された、同じ駅の乗換候補・赤線・候補解除を反映しました。
- 長文の会話コードは取得時に末尾が省略されたため、確認できた仕様に基づいて実装しました。
  教室PCの未pushファイルとの完全一致を確認したものではありません。
- ズームに赤線を追従させ、非表示パネルがCSSで表示される問題も修正しました。
- 帰りは神戸→茨木の駅順で、希望出発時刻以降の神戸発の最大10本を表示します。
  神戸時刻が空欄の途中駅始発列車は、従来の検索仕様どおり対象外です。
- 行き・帰りで描画処理を共通化しました。帰りの深夜帯（0〜2時台）は翌日として配置し、
  時間軸に「翌日」を表示します。CSVの14時台を翌日扱いする不具合も修正しました。
- CSVは変更していません。現データの神戸発最終列車は23:47で、それより後の検索は範囲外になります。
- 自宅・教室で共有する開発先： https://github.com/mappoll/TimeTable
  教室側の基準コミットに、この開発での変更を加えたコードを掲載しています。

## 確認結果

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
```

既存23件と追加5件の計28テスト成功。Edgeで行き・帰りの検索、列車選択、乗換赤線、ズーム時の接続位置、両方の解除、
同じ駅がない場合、帰り検索、範囲外時刻を確認し、JavaScriptエラーなし。
幅390pxで画面確認済み。スマホ実機でのピンチ操作は未確認です。

教室へ戻る際は、教室PCの未push分とこの変更を比較してから統合してください。
以後、同じリポジトリを使う場合は作業前にpullし、作業後にコミット・pushします。
